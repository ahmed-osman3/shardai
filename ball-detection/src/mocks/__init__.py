"""Mock implementations of surrounding system components.

These provide realistic interfaces and plausible-looking outputs so the pipeline
can run end-to-end while the real CV/backend modules are built separately.
"""
